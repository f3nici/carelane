// `escapeLike` moved to `@carelane/core` (shared, pure helper). Re-exported here
// so server-only modules importing `../utils/sql.js` keep working unchanged.
export { escapeLike } from '@carelane/core'
